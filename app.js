const gallery=document.querySelector('#gallery');const tabs=document.querySelectorAll('.tabs button');let mode='annotated';function render(){gallery.innerHTML='';window.HELIX_PHOTOS.forEach((p,i)=>{const useAnn=mode==='annotated'&&p.annotated;const path=`images/${useAnn?'annotated':'originals'}/${p.file}`;const el=document.createElement('article');el.className='photo';el.innerHTML=`<img loading="lazy" src="${path}" alt="${p.title}"><div class="cap"><span class="badge ${useAnn?'ann':''}">${useAnn?'Annotated':'Original'}</span><h3>${p.title}</h3><small>${p.caption}</small></div>`;el.querySelector('img').onclick=()=>openLightbox(path,p.title);gallery.appendChild(el);});}function openLightbox(src,alt){const lb=document.querySelector('.lightbox');const im=lb.querySelector('img');im.src=src;im.alt=alt;lb.classList.add('open')}document.querySelector('.lightbox button').onclick=()=>document.querySelector('.lightbox').classList.remove('open');document.querySelector('.lightbox').onclick=e=>{if(e.target.classList.contains('lightbox'))e.currentTarget.classList.remove('open')};tabs.forEach(b=>b.onclick=()=>{tabs.forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.mode;render()});render();